#pragma once
#include <string>
#include <vector>

class World {
public:
	//std::map<unsigned long, Thing> obs; // id, obj
	std::string name;
	//std::vector<std::vector<std::vector<Cell>>> grid; // этаж | y | x

};
